{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"templatepy","text":"<p>Copier template for modern Python packages.</p>"},{"location":"#stack","title":"Stack","text":"Tool Role uv Dependency management, virtual environments, running tools hatchling + hatch-vcs Build backend; version derived from git tags commitizen Conventional Commits enforcement; auto version bump on merge ruff Linting and formatting mypy Static type checking pytest + pytest-cov Testing with coverage MkDocs Material Documentation (this site)"},{"location":"#requirements","title":"Requirements","text":"<ul> <li>Python \u2265 3.12</li> <li>uv</li> <li>copier \u2265 9.0</li> </ul> <pre><code>uv tool install copier\n</code></pre>"},{"location":"#create-a-new-project","title":"Create a new project","text":"<pre><code>copier copy gh:larsrollik/templatepy my-new-project\ncd my-new-project\ngit init &amp;&amp; git add -A &amp;&amp; git commit -m \"chore: initial commit from templatepy\"\nuv sync --extra dev\nuv run pre-commit install --hook-type pre-commit --hook-type commit-msg\n</code></pre>"},{"location":"#apply-template-updates-to-an-existing-project","title":"Apply template updates to an existing project","text":"<pre><code>cd my-existing-project\ncopier update\n</code></pre> <p>Copier reads <code>.copier-answers.yml</code> (committed in the generated repo) to know what answers were given and which template version was used.</p>"},{"location":"#day-to-day-commands","title":"Day-to-day commands","text":"<pre><code>cz commit                          # structured commit (enforces Conventional Commits)\nuv run pytest                      # run tests\nuv run pre-commit run --all-files  # run all lint checks manually\n</code></pre> <p>Version bumping and releasing are handled automatically by <code>bump.yml</code> and <code>release.yml</code> on every merge to <code>main</code>. Manual override:</p> <pre><code>cz bump &amp;&amp; git push --follow-tags\n</code></pre>"},{"location":"#project-structure-generated","title":"Project structure (generated)","text":"<pre><code>my-project/\n\u251c\u2500\u2500 src/\n\u2502   \u2514\u2500\u2500 my_project/\n\u2502       \u251c\u2500\u2500 __init__.py          # version via importlib.metadata\n\u2502       \u2514\u2500\u2500 py.typed             # PEP 561 typed marker\n\u251c\u2500\u2500 tests/\n\u2502   \u251c\u2500\u2500 conftest.py\n\u2502   \u251c\u2500\u2500 unit/\n\u2502   \u2514\u2500\u2500 integration/\n\u251c\u2500\u2500 .github/\n\u2502   \u2514\u2500\u2500 workflows/\n\u2502       \u251c\u2500\u2500 ci.yml               # lint on push; tests on PR to main\n\u2502       \u251c\u2500\u2500 bump.yml             # auto cz bump on merge to main\n\u2502       \u251c\u2500\u2500 release.yml          # on v* tag: GitHub release + PyPI (OIDC)\n\u2502       \u2514\u2500\u2500 docs.yml             # deploy MkDocs to GitHub Pages on push to main\n\u251c\u2500\u2500 pyproject.toml\n\u251c\u2500\u2500 CITATION.cff                 # citation metadata for Zenodo + GitHub\n\u251c\u2500\u2500 .pre-commit-config.yaml\n\u2514\u2500\u2500 LICENSE\n</code></pre>"},{"location":"branch-protection/","title":"Branch protection","text":"<p>Configure branch protection rules on GitHub to enforce the CI gate and allow automated version bumping.</p>"},{"location":"branch-protection/#recommended-rules-for-main","title":"Recommended rules for <code>main</code>","text":"<p>Repository \u2192 Settings \u2192 Branches \u2192 Add rule, branch name pattern: <code>main</code></p> Setting Value Reason Require a pull request before merging \u2713 No direct pushes to main Required approvals 1+ At least one review Dismiss stale reviews on new commits \u2713 Re-review after force-push Require status checks to pass \u2713 Blocks merge on CI failure Required status checks <code>CI</code> The aggregate job in <code>ci.yml</code> Require branches to be up to date \u2713 No stale merges Require linear history \u2713 Keeps git log readable"},{"location":"branch-protection/#allow-bumpyml-to-push-back-to-main","title":"Allow <code>bump.yml</code> to push back to <code>main</code>","text":"<p><code>bump.yml</code> creates a version tag and pushes it after merging. GitHub's default branch protection blocks this. Fix:</p> <p>Repository \u2192 Settings \u2192 Branches \u2192 main rule \u2192 Allow specified actors to bypass required pull requests</p> <p>Add: <code>github-actions[bot]</code></p> <p>Without this, the bump workflow will fail with a 403 when trying to push the tag.</p>"},{"location":"branch-protection/#required-status-check-name","title":"Required status check name","text":"<p>The aggregate job in <code>ci.yml</code> is named <code>CI</code>. This is what to enter in the required status checks field. It passes only when <code>lint</code> and <code>secrets-scan</code> both succeed.</p>"},{"location":"branch-protection/#rulesets-modern-alternative","title":"Rulesets (modern alternative)","text":"<p>GitHub now offers Rulesets (Repository \u2192 Settings \u2192 Rules \u2192 Rulesets) as a more flexible replacement for classic branch protection. Rulesets support bypass lists, actor-based rules, and can be applied to tag patterns (useful for protecting <code>v*</code> tags from deletion or force-push).</p>"},{"location":"gpg-signing/","title":"GPG signing","text":"<p>Signing commits and tags is optional but recommended for open-source projects. GitHub shows a \"Verified\" badge on signed commits.</p>"},{"location":"gpg-signing/#generate-a-key","title":"Generate a key","text":"<pre><code>gpg --full-generate-key\n</code></pre> <p>Recommended options:</p> <ul> <li>Key type: <code>(9) ECC (sign and encrypt)</code> \u2192 Curve 25519</li> <li>Expiry: <code>2y</code> (rotate every two years)</li> <li>Name and email matching your GitHub account</li> </ul>"},{"location":"gpg-signing/#find-your-key-id","title":"Find your key ID","text":"<pre><code>gpg --list-secret-keys --keyid-format LONG\n</code></pre> <p>Output:</p> <pre><code>sec   ed25519/&lt;KEY_ID&gt; 2024-01-01 [SC] [expires: 2026-01-01]\n      ABCDEF1234567890ABCDEF1234567890ABCDEF12\nuid   [ultimate] Your Name &lt;you@example.com&gt;\nssb   cv25519/&lt;SUBKEY_ID&gt; 2024-01-01 [E]\n</code></pre> <p>The <code>&lt;KEY_ID&gt;</code> is the 16-character hex string after <code>ed25519/</code>.</p>"},{"location":"gpg-signing/#configure-git-to-use-the-key","title":"Configure git to use the key","text":"<pre><code>git config --global user.signingkey &lt;KEY_ID&gt;\ngit config --global commit.gpgsign true\ngit config --global tag.gpgSign true\n</code></pre>"},{"location":"gpg-signing/#add-the-public-key-to-github","title":"Add the public key to GitHub","text":"<pre><code>gpg --armor --export &lt;KEY_ID&gt;\n</code></pre> <p>Copy the output (including <code>-----BEGIN PGP PUBLIC KEY BLOCK-----</code>).</p> <p>Go to GitHub \u2192 Settings \u2192 SSH and GPG keys \u2192 New GPG key and paste it.</p>"},{"location":"gpg-signing/#common-issues","title":"Common issues","text":"<p>GPG hangs and never asks for passphrase</p> <pre><code>export GPG_TTY=$(tty)\n</code></pre> <p>Add this to your shell profile (<code>~/.zshrc</code> or <code>~/.bashrc</code>) to make it permanent.</p> <p><code>error: gpg failed to sign the data</code></p> <pre><code>gpgconf --kill gpg-agent\ngpg-agent --daemon\n</code></pre> <p>Wrong key used / multiple keys</p> <p>Check which key git is using:</p> <pre><code>git config --global user.signingkey\n</code></pre> <p>List all available secret keys:</p> <pre><code>gpg --list-secret-keys\n</code></pre> <p>Key expired</p> <p>Extend expiry without creating a new key:</p> <pre><code>gpg --edit-key &lt;KEY_ID&gt;\n# at the gpg&gt; prompt:\nexpire\n# follow prompts, then:\nsave\n# re-export and update the key on GitHub\ngpg --armor --export &lt;KEY_ID&gt;\n</code></pre>"},{"location":"gpg-signing/#disabling-signing-temporarily","title":"Disabling signing temporarily","text":"<pre><code>git commit --no-gpg-sign -m \"message\"\ngit tag --no-sign v1.0.0\n</code></pre> <p>Or disable globally:</p> <pre><code>git config --global commit.gpgsign false\ngit config --global tag.gpgSign false\n</code></pre>"},{"location":"local-ci/","title":"Local CI with act","text":"<p>act runs GitHub Actions workflows locally using Docker. Useful for debugging CI without pushing.</p>"},{"location":"local-ci/#install","title":"Install","text":"<pre><code># macOS\nbrew install act\n\n# Linux\ncurl -s https://raw.githubusercontent.com/nektos/act/master/install.sh | sudo bash\n\n# Windows (winget)\nwinget install nektos.act\n</code></pre> <p>Requires Docker Desktop to be running.</p>"},{"location":"local-ci/#usage","title":"Usage","text":"<pre><code># Run all workflows triggered by push\nact push\n\n# Run a specific workflow\nact push -W .github/workflows/ci.yml\n\n# Run with verbose output\nact push --verbose\n\n# List available workflows and jobs\nact -l\n\n# Run a specific job\nact push -j lint\n</code></pre>"},{"location":"local-ci/#first-run","title":"First run","text":"<p>On first run, <code>act</code> asks which Docker image size to use:</p> <ul> <li>Micro \u2014 minimal, fast to download, may lack some tools</li> <li>Medium \u2014 recommended for most workflows</li> <li>Large \u2014 full GitHub Actions environment, ~20 GB</li> </ul> <p>For this template's workflows, Medium is sufficient.</p>"},{"location":"local-ci/#known-limitations","title":"Known limitations","text":"<ul> <li><code>act</code> doesn't support all GitHub Actions features (e.g. <code>workflow_run</code>, some contexts)</li> <li>Some actions that call back to GitHub APIs may behave differently</li> <li><code>gitleaks</code> uses the <code>golang</code> language in its pre-commit hook and requires Go to be installed locally \u2014 see Pre-commit hooks</li> <li>OIDC trusted publishing (<code>uv publish --trusted-publishing auto</code>) does not work locally; skip the publish step when testing <code>release.yml</code> with act</li> </ul>"},{"location":"pre-commit/","title":"Pre-commit hooks","text":"<p>This template ships a <code>.pre-commit-config.yaml</code> with the following hooks:</p> Hook Purpose <code>pre-commit-hooks</code> Trailing whitespace, line endings, YAML/TOML syntax, merge conflict markers <code>ruff</code> Linting with auto-fix <code>ruff-format</code> Formatting <code>mypy</code> Static type checking <code>commitizen</code> Conventional Commits enforcement on commit messages <code>gitleaks</code> Secret scanning \u2014 blocks commits containing API keys, tokens, passwords"},{"location":"pre-commit/#setup","title":"Setup","text":"<pre><code>uv run pre-commit install --hook-type pre-commit --hook-type commit-msg\n</code></pre> <p>Run all hooks manually against every file:</p> <pre><code>uv run pre-commit run --all-files\n</code></pre>"},{"location":"pre-commit/#gitleaks-handling-false-positives","title":"Gitleaks \u2014 handling false positives","text":"<p>Gitleaks scans staged content against a large ruleset of known secret patterns. Occasionally it flags test fixtures, example keys, or other non-sensitive strings.</p>"},{"location":"pre-commit/#option-1-inline-allowlist-single-occurrence","title":"Option 1 \u2014 inline allowlist (single occurrence)","text":"<p>Add a <code>gitleaks:allow</code> comment on the offending line:</p> <pre><code>EXAMPLE_API_KEY = \"AKIAIOSFODNN7EXAMPLE\"  # gitleaks:allow\n</code></pre>"},{"location":"pre-commit/#option-2-allowlist-in-gitleakstoml-recurring-pattern","title":"Option 2 \u2014 allowlist in <code>.gitleaks.toml</code> (recurring pattern)","text":"<p>The template includes a <code>.gitleaks.toml</code> at the repo root. Uncomment and extend the relevant section:</p> <pre><code>[allowlist]\ndescription = \"Global allowlist\"\n\n# Ignore an entire directory (e.g. test fixtures)\npaths = [\n  '''tests/fixtures/''',\n]\n\n# Ignore a specific pattern everywhere\nregexes = [\n  '''EXAMPLE_[A-Z0-9]{20}''',\n]\n</code></pre> <p>See the gitleaks allowlist docs for the full set of options (per-rule allowlists, commit SHAs, etc.).</p>"},{"location":"pre-commit/#option-3-skip-for-one-commit-last-resort","title":"Option 3 \u2014 skip for one commit (last resort)","text":"<pre><code>SKIP=gitleaks git commit -m \"chore: ...\"\n</code></pre> <p>Use this sparingly \u2014 it bypasses the check entirely for that commit.</p>"},{"location":"pre-commit/#gitleaks-go-requirement","title":"Gitleaks \u2014 Go requirement","text":"<p>Gitleaks uses <code>language: golang</code> in its pre-commit hook definition. pre-commit will download and compile it automatically, but Go must be installed on the machine.</p> <pre><code># macOS\nbrew install go\n\n# or via the official installer: https://go.dev/dl/\n</code></pre>"},{"location":"pypi-publishing/","title":"PyPI publishing","text":"<p>The <code>release.yml</code> workflow publishes to PyPI automatically when a <code>v*</code> tag is pushed. Authentication uses OIDC trusted publishing \u2014 no API token stored in GitHub secrets.</p>"},{"location":"pypi-publishing/#one-time-setup-on-pypi","title":"One-time setup on PyPI","text":"<ol> <li>Register at pypi.org and enable 2FA (required since 2024)</li> <li>Go to Account \u2192 Publishing \u2192 Add a new pending publisher</li> <li>Fill in:</li> <li>PyPI project name: your package name</li> <li>GitHub owner: your GitHub username or org</li> <li>Repository name: your repo name</li> <li>Workflow filename: <code>release.yml</code></li> <li>Environment name: (leave blank)</li> <li>Click Add</li> </ol> <p>That's it. The workflow handles authentication automatically \u2014 no token, no secret.</p>"},{"location":"pypi-publishing/#trigger-a-release","title":"Trigger a release","text":"<p>Releases are triggered automatically by <code>bump.yml</code> on every merge to <code>main</code>. To trigger manually:</p> <pre><code>cz bump\ngit push --follow-tags\n</code></pre> <p>The <code>release.yml</code> workflow will:</p> <ol> <li>Build wheel + sdist with <code>uv build</code></li> <li>Create a GitHub release with the dist files attached</li> <li>Publish to PyPI with <code>uv publish --trusted-publishing auto</code></li> </ol>"},{"location":"pypi-publishing/#test-pypi","title":"Test PyPI","text":"<p>To publish to test.pypi.org first, add a pending publisher there (same steps). Then temporarily edit <code>release.yml</code>:</p> <pre><code>- name: Publish to Test PyPI\n  run: uv publish --trusted-publishing auto --publish-url https://test.pypi.org/legacy/\n</code></pre>"},{"location":"pypi-publishing/#manual-publish-local","title":"Manual publish (local)","text":"<p>Requires a PyPI API token:</p> <pre><code>uv build\nUV_PUBLISH_TOKEN=pypi-... uv publish\n</code></pre>"},{"location":"pypi-publishing/#zenodo-archiving","title":"Zenodo archiving","text":"<p>If the Zenodo webhook is enabled, every GitHub release automatically creates a Zenodo record. See <code>CITATION.cff</code> for the metadata format. After the first release, copy the concept DOI from Zenodo and add it to <code>CITATION.cff</code>.</p>"},{"location":"release-workflow/","title":"Release workflow","text":""},{"location":"release-workflow/#branch-topology","title":"Branch topology","text":"<pre><code>main  \u2190\u2500\u2500 PR merges from feature/bug/hotfix branches\nfeature/*, fix/*, chore/*  \u2190\u2500\u2500 created from main, deleted after merge\n</code></pre> <p>Tags on <code>main</code> are the release record. There is no <code>prod</code> branch \u2014 <code>git checkout v1.2.3</code> gives you any released state.</p>"},{"location":"release-workflow/#step-by-step","title":"Step-by-step","text":"<pre><code># 1. Create a branch\ngit checkout main &amp;&amp; git pull\ngit checkout -b feature/my-feature\n\n# 2. Do your work, committing with cz\ncz commit        # interactive prompt enforcing Conventional Commits\n\n# 3. Push branch and open PR\ngit push -u origin feature/my-feature\ngh pr create --base main --title \"feat: my feature\"\n\n# 4. CI runs: lint on push, tests on PR to main \u2014 merge is blocked until green\n\n# 5. Merge PR (rebase or squash)\ngh pr merge --rebase --delete-branch\n\n# 6. bump.yml fires automatically on push to main:\n#    \u2192 reads commits since last tag\n#    \u2192 runs cz bump --yes to determine patch/minor/major\n#    \u2192 creates version tag (e.g. v1.3.0) and pushes it\n\n# 7. release.yml fires on the new tag:\n#    \u2192 builds wheel + sdist\n#    \u2192 creates GitHub release with dist files attached\n#    \u2192 publishes to PyPI via OIDC trusted publishing (no stored token)\n#    \u2192 Zenodo webhook archives the release (if enabled)\n</code></pre>"},{"location":"release-workflow/#how-version-bumps-are-determined","title":"How version bumps are determined","text":"<p>Commitizen reads all commits since the previous tag:</p> Commits contain Bump only <code>fix:</code>, <code>docs:</code>, <code>chore:</code>, <code>refactor:</code>, <code>test:</code> patch <code>0.0.x</code> at least one <code>feat:</code> minor <code>0.x.0</code> <code>BREAKING CHANGE:</code> footer or <code>feat!:</code>/<code>fix!:</code> major <code>x.0.0</code> <p>If there are no bumpable commits since the last tag, <code>bump.yml</code> exits silently \u2014 no error, no tag.</p>"},{"location":"release-workflow/#manual-bump-override","title":"Manual bump (override)","text":"<p>Automated bumping covers most cases. Override when needed:</p> <pre><code>cz bump --increment PATCH\ncz bump --increment MINOR\ncz bump --increment MAJOR\ngit push --follow-tags\n</code></pre>"},{"location":"release-workflow/#conventional-commits-format","title":"Conventional Commits format","text":"<pre><code>&lt;type&gt;[optional scope]: &lt;short description&gt;\n\n[optional body]\n\n[optional footer: BREAKING CHANGE: ...]\n</code></pre> <p>Common types: <code>feat</code>, <code>fix</code>, <code>docs</code>, <code>chore</code>, <code>refactor</code>, <code>test</code>, <code>ci</code>, <code>build</code>.</p>"},{"location":"release-workflow/#common-issues","title":"Common issues","text":"<p><code>bump.yml</code> pushed a tag but <code>release.yml</code> didn't trigger</p> <p>GitHub prevents downstream <code>push</code> triggers when <code>GITHUB_TOKEN</code> is the actor. If this happens, store a PAT as <code>BUMP_TOKEN</code> and set <code>token: ${{ secrets.BUMP_TOKEN }}</code> in the checkout step of <code>bump.yml</code>.</p> <p><code>cz bump</code> fails with exit code 128</p> <ul> <li>Detached HEAD \u2192 <code>git checkout main</code> first</li> <li>Uncommitted changes \u2192 commit or stash first</li> <li>Tag already exists \u2192 <code>git tag</code> to list, <code>git tag -d &lt;tag&gt;</code> to remove locally</li> </ul> <p>GPG signing failure</p> <p>See GPG signing.</p>"}]}